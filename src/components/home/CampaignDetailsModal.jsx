import React from "react";
export default function CampaignDetailsModal({ campanha, onClose, onDuplicate, onRequestDelete, formatarDataHistorico }) {
  if(!campanha) return null;
  return <div className="popup-overlay" role="dialog" aria-modal="true"><div className="popup-card popup-card-historico-pro"><div className="popup-header popup-header-pro"><div><div className="popup-eyebrow">Histórico de campanhas</div><h2>{campanha.titulo||"Campanha"}</h2><p className="popup-subtitle">Consulta os detalhes e duplica a campanha para voltar a editar.</p></div><button type="button" className="popup-close" onClick={onClose}>×</button></div><div className="ai-popup-scroll"><div className="popup-status-row"><span className="popup-chip">Criada em: {formatarDataHistorico(campanha.criadoEm)}</span><span className="popup-chip">Artigos: {campanha.totalArtigos||0}</span><span className="popup-chip">Formato base: {String(campanha.formatoEtiqueta||"a6").toUpperCase()}</span><span className="popup-chip">Validade: {campanha.anoValidade||"-"}</span></div><div className="historico-popup-list">{Array.isArray(campanha.dados)&&campanha.dados.map((item)=><div key={item.id||`${campanha.id}-${item.codigo}`} className="historico-popup-item"><div className="historico-popup-main"><strong>{item.codigo||"-"}</strong><span>{item.descricao||"-"}</span></div><div className="historico-popup-prices"><span>Antes: {item.antes??"-"}</span><span>Atual: {item.atual??"-"}</span></div></div>)}</div></div><div className="popup-actions popup-actions-pro"><button type="button" className="btn btn-primary" onClick={()=>onDuplicate(campanha)}>Duplicar campanha</button><button type="button" className="btn btn-secondary" onClick={()=>onRequestDelete(campanha)}>Apagar</button><button type="button" className="btn btn-secondary" onClick={onClose}>Fechar</button></div></div></div>;
}
