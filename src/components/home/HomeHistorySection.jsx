import React from "react";
export default function HomeHistorySection({ historicoPreview, onOpenCampaign, formatarDataHistorico, formatarAutorCampanha }) {
  return <div className="home-section"><div className="table-card-header"><h2>Histórico de campanhas</h2></div><div className="home-history-wrap">{historicoPreview.length===0 ? <div className="home-history-empty">Ainda não existem campanhas guardadas.</div> : <div className="home-history-grid">{historicoPreview.filter((campanha)=>campanha&&campanha.id).map((campanha)=><button key={campanha.id} type="button" className="home-history-card" onClick={()=>onOpenCampaign(campanha)}><div className="home-history-top"><div><strong>{campanha.titulo||"PROMO"}</strong><span>{formatarDataHistorico(campanha.criadoEm)}</span></div><span className="home-history-format">{String(campanha.formatoEtiqueta||"a6").toUpperCase()}</span></div><div className="home-history-meta"><span>{campanha.totalArtigos||0} artigos</span><span>Validade: {campanha.anoValidade||"-"}</span><span>Criado por: {formatarAutorCampanha(campanha)}</span></div><div className="home-history-preview">{Array.isArray(campanha.dados)&&campanha.dados.slice(0,2).map((item)=><p key={item.id||`${campanha.id}-${item.codigo}`}>{item.codigo} — {item.descricao}</p>)}</div></button>)}</div>}</div></div>;
}
